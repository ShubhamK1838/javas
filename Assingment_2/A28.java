package Assingment_2;
 


    // --->  don't write . 


// 8) Design GUP form that will take employee information from the user
// like (eid, name, DOB, contact no) .Perform the operations like insert,
// delete, update, search with the Database'

/*
 * 
 import java.sql.*; 
 import javax.swing.*;
 
 import java.awt.Dimension;
 import java.awt.FlowLayout;
 import java.awt.event.ActionListener; 
 
 /*
 * create table  employees (id int, name varchar(20),  dob varchar(20), phone(13)) ; 
 

public class A28  extends JFrame {
    
    JTextField emp[]; 
    JButton operations[]; 
    ActionListener listener; 
    
    public A28()
    {
        setSize(600,600); 
        setDefaultCloseOperation(3); 
        setLayout(new FlowLayout()); 
        initFields(); 
        
        setVisible(true); 
    }
    
    public void initFields()
    {
        String fields[]={"Id","Name","dob", "contact"};
        
        emp=new JTextField[fields.length]; 
        operations=new JButton[fields.length]; 
        for(int i=0; i<emp.length; i++)
        {
            add(new JLabel(fields[i])); 
            add( emp[i]=new JTextField()); 
            emp[i].setPreferredSize(new Dimension(90,40)); 
        }
        fields=new String[]{"Insert","Update","Delete","Search" }; 
        for(int i=0; i<fields.length; i++)
        {
            add( operations[i]=new JButton(fields[i])); 
            operations[i].addActionListener(listener); 
            
        }
        
        
    }
    
    public static void main(String ar[])
    {
        
        
        new A28(); 
    }
}


*/
